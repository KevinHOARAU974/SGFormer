[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "sgformer"
version = "0.0.0"
description = "SGFormer paper code packaged as library"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["sgformer", "sgformer.*"]